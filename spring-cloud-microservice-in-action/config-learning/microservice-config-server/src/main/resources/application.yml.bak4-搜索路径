server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://git.oschina.net/it-much/config-repo-51cto-video      # 公用
          search-paths:
            - foo   # foo路径
            - bar   # bar路径
